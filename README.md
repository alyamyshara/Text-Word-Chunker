# Text-Word-Chunker
A Streamlit web app that splits long text into chunks of N words using a simple Python chunking function. Users can input text, choose the chunk size, preview individual chunks, and display all generated chunks.
